info:
  name: Save user
  type: http
  seq: 1

http:
  method: POST
  url: "{{URL}}/users"
  body:
    type: json
    data: |
      {
        "username": "robert_duchmol",
        "email": "robertduchmol@example.com",
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
