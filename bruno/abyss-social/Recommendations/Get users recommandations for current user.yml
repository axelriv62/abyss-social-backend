info:
  name: Get users recommandations for current user
  type: http
  seq: 1

http:
  method: GET
  url: "{{URL}}/recommendations/users"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
