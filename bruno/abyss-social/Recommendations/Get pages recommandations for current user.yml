info:
  name: Get pages recommandations for current user
  type: http
  seq: 3

http:
  method: GET
  url: "{{URL}}/recommendations/pages"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
