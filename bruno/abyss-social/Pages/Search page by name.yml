info:
  name: Search page by name
  type: http
  seq: 6

http:
  method: GET
  url: "{{URL}}/pages/search?query=Test"
  params:
    - name: query
      value: Test
      type: query
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
