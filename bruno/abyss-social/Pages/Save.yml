info:
  name: Save
  type: http
  seq: 1

http:
  method: POST
  url: "{{URL}}/pages"
  body:
    type: json
    data: |
      {
        "name": "Test",
        "tags": ["tag1", "tag2"],
        "posts": []
      }
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
