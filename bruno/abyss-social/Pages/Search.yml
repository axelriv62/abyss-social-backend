info:
  name: Search
  type: http
  seq: 7

http:
  method: GET
  url: "{{URL}}/pages/search?query=Page"
  params:
    - name: query
      value: Page
      type: query
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
