info:
  name: Get user by id
  type: http
  seq: 17

http:
  method: GET
  url: "{{URL}}/users/search?username=robert"
  params:
    - name: username
      value: robert
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
