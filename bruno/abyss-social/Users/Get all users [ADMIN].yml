info:
  name: Get all users [ADMIN]
  type: http
  seq: 15

http:
  method: GET
  url: "{{URL}}/users/all"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
