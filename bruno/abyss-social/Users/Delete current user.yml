info:
  name: Delete current user
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{URL}}/users"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
