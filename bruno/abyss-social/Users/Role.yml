info:
  name: Role
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{URL}}/users/role/69ccf2aa15852ad1e8d5b0a2?newRole=ADMIN"
  params:
    - name: newRole
      value: ADMIN
      type: query
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
