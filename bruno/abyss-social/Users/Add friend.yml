info:
  name: Add friend
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{URL}}/users/friends/add?friendId=69ce30c940ec30873ed63a8b"
  params:
    - name: friendId
      value: 69ce30c940ec30873ed63a8b
      type: query
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
