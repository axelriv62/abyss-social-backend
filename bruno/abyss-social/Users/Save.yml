info:
  name: Save
  type: http
  seq: 1

http:
  method: POST
  url: "{{URL}}/users"
  body:
    type: json
    data: |
      {
        "username": "gerard_martin",
        "email": "gerardmartin@example.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
