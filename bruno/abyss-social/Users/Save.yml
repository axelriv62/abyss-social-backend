info:
  name: Save
  type: http
  seq: 1

http:
  method: POST
  url: "{{URL}}/users"
  body:
    type: json
    data: |
      {
        "username": "robert_duchmol2",
        "email": "robertduchmol@example.com"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
