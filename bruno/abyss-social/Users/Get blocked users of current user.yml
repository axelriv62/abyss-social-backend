info:
  name: Get blocked users of current user
  type: http
  seq: 16

http:
  method: GET
  url: "{{URL}}/users/blocked"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
