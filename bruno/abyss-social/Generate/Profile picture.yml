info:
  name: Profile picture
  type: http
  seq: 7

http:
  method: POST
  url: "{{URL}}/generate/profile-picture"
  body:
    type: json
    data: |-
      {
        "prompt": "post"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
