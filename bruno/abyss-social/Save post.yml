info:
  name: Save post
  type: http
  seq: 2

http:
  method: POST
  url: "{{URL}}/posts"
  body:
    type: json
    data: |
      {
        "content": "Post test",
        "comments": [],
        "like": 0,
        "dislike": 0,
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
