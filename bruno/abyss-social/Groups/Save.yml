info:
  name: save
  type: http
  seq: 1

http:
  method: post
  url: http://localhost:8080/groups
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |
      {
        "name": "Groupe de Bylel Longelin",
        "tags": ["tag1", "tag5", "tag7777"],
        "posts": ["69cab182f9aa8f8710d001e3"]
      }
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
