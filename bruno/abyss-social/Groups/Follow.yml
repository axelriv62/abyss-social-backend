info:
  name: Follow
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{URL}}/groups/69cbdb1807d55ff1ba4d3966/follow"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
