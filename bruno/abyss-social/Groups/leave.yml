info:
  name: leave
  type: http
  seq: 6

http:
  method: POST
  url: "{{URL}}/groups/groupID/leave"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
