info:
  name: search by name
  type: http
  seq: 6

http:
  method: GET
  url: "{{URL}}/groups/search?query="
  params:
    - name: query
      value: ""
      type: query
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
