info:
  name: Search posts by content
  type: http
  seq: 11

http:
  method: GET
  url: "{{URL}}/posts/search?query=plongée"
  params:
    - name: query
      value: plongée
      type: query
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
