info:
  name: Get all posts [TESTS]
  type: http
  seq: 14

http:
  method: GET
  url: "{{URL}}/posts/all?size=30"
  params:
    - name: size
      value: "30"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
