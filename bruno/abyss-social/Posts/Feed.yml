info:
  name: Feed
  type: http
  seq: 11

http:
  method: GET
  url: "{{URL}}/posts/feed?limit=30"
  params:
    - name: limit
      value: "30"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
