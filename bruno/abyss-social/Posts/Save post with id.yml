info:
  name: Save post with id
  type: http
  seq: 2

http:
  method: POST
  url: "{{URL}}/posts"
  body:
    type: json
    data: |-
      {
        "content": "Essai pour les commentaires",
        "comments": [],
        "likes": [],
        "dislikes": []
      }
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
