info:
  name: Search by date
  type: http
  seq: 8

http:
  method: GET
  url: "{{URL}}/posts/search/by-date?date=2026-04-01"
  params:
    - name: date
      value: 2026-04-01
      type: query
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
