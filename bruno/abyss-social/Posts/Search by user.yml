info:
  name: Search by user
  type: http
  seq: 10

http:
  method: GET
  url: "{{URL}}/posts/search/by-creator?username=robertduchmol"
  params:
    - name: username
      value: robertduchmol
      type: query
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
