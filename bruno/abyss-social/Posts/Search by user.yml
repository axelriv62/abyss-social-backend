info:
  name: Search by user
  type: http
  seq: 10

http:
  method: GET
  url: "{{URL}}/posts/search/by-date?date=2026-03-31"
  params:
    - name: date
      value: 2026-03-31
      type: query
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
