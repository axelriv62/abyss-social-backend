info:
  name: Me
  type: http
  seq: 4

http:
  method: POST
  url: "{{URL}}/auth/me"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
